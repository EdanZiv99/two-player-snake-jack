class Main {
    function void main() {
        var Game game;
        do Snake.init();  // Initialize Snake static fields
        let game = Game.new();
        do game.run();
        do game.dispose();
        return;
    }
}